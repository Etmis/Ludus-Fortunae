using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AddPlayerModal : MonoBehaviour
{
    /*public GameObject modalWindow;      // Odkaz na panel modálního okna
    public InputField userInputField;   // Input Field pro uživatelský text
    public Text feedbackText;           // Text pro zobrazení uloženého textu nebo potvrzení

    private string savedText;           // Uložený text od uživatele

    // Funkce pro zobrazení modálního okna
    public void ShowModal()
    {
        modalWindow.SetActive(true);
        userInputField.text = "";       // Vyprázdní InputField pøi otevøení
    }

    // Funkce pro uložení textu z Input Field
    public void SaveText()
    {
        savedText = userInputField.text;
        feedbackText.text = "Text uložen: " + savedText; // Volitelné: zobrazení zpìtné vazby
        modalWindow.SetActive(false);   // Skryje modální okno
    }*/
}
